PORT=5000

# Firebase configuration
# Option A: Path to service account JSON key file (relative to server directory or absolute path)
FIREBASE_SERVICE_ACCOUNT_PATH=./service-account.json

# Option B: Or, specify the service account credentials directly below
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
